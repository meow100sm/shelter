# Глава 2 (архитектура и БД) — актуализация под текущий проект — 2026-05-26

Проверяемый документ: text.txt

Цель: привести разделы 2.3–2.4 к тому, как реально устроен проект (Django apps + Docker Compose + PostgreSQL) и согласовать вставку диаграмм.

## 1) Куда вставить диаграммы (глава 2)

Принятая нумерация рисунков для раздела 2.3–2.4:
- Рисунок 2.1 — контекстная (структурная) схема: `струк.jpg`
- Рисунок 2.2 — диаграмма компонентов: `комп.png`
- Рисунок 2.3 — диаграмма развёртывания: `разв.jpg`
- Рисунок 2.4 — диаграмма классов доменной модели: `class_diagram.png`
- Рисунок 2.5 — ER-диаграмма БД: `ер.jpg`

В text.txt добавлены явные плейсхолдеры вида: `[Вставить рисунок: ...]`.

## 2) Что исправлено в 2.3 (архитектура)

- Убран отдельный подпункт «Общая структурная схема системы».
- Контекстная схема (Рисунок 2.1) перенесена внутрь 2.3.2 как иллюстрация трёхуровневой архитектуры.
- «Диаграмма компонентов» приведена к реальным приложениям Django: `accounts`, `animals`, `reports` (без вымышленных компонентов вроде DatabaseConnector).
- «Диаграмма развёртывания» приведена к реальному Docker Compose: контейнеры `web` (Django), `db` (PostgreSQL) и `reminders` (management-команда отправки напоминаний).
- Добавлен новый подпункт «Диаграмма классов UML» (Рисунок 2.4), соответствующий моделям Django ORM.

## 3) Что исправлено в 2.4 (база данных)

- ER-диаграмма перенумерована на Рисунок 2.5 и добавлен плейсхолдер вставки `ер.jpg`.
- Описание связи User–Animal уточнено: связь отражает автора создания карточки животного (`created_by_id`).
- Таблица 2.3 (animals) приведена к текущей модели:
  - `id` как BIGSERIAL (Django BigAutoField)
  - `species` как VARCHAR(10)
  - `sex` как CHAR(1) и NOT NULL
  - `status` как VARCHAR(20)
  - добавлены `created_by_id`, `updated_at`
- Утверждения про индексы/ограничения скорректированы: уникальность `unique_id` и индексы для FK создаются ORM/миграциями; дополнительные индексы могут быть добавлены при необходимости.

## 4) Что ещё стоит держать в голове (если править дальше главу 2)

- Нефункциональные требования про HTTPS/Nginx/Gunicorn и про замеры производительности не подтверждаются репозиторием (это скорее про production-окружение).
- Про резервное копирование корректнее писать как «в проекте предусмотрены скрипты backup.sh/backup.ps1; запуск настраивается средствами ОС».
- Если в выводах главы 2 встречаются утверждения про CHECK-ограничения (перечисления статусов/пола), их лучше формулировать мягче (как предмет дальнейшего усиления целостности), если такие CHECK не добавлялись миграциями.
